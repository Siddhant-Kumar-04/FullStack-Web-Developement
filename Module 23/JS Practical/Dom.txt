Dom Manipulation

One of the most fundamental use-case of JS is to able to manipulate and control DOM (Document Object Model)

What is the meaning of the word DOM manipulation?
Ans) When we say, manipulating the DOM, 
we mean that using JS we 
should be able to control what
elements are being added/removed to the dom,
they behave in terms of user interaction,
how their look and feel is and everything 
else related to thes HTML elements

How to achieve DOM Manipulation?
So our browser 
provide us a lot of 
function and objects
using which we can control the whole DOM.
important thing to note is that these
function are not native to JS,
instead they are provided by the 
browser(which is the runtime environment).

Some of the most important function are.
1)document.getElementById
2)document.getElementByClassName
3)document.querySelector
4)document.getElementByTagName
...and More

In order to understand DOM manipulation
we will learn by
building a samll teir maker app.

What feature do we expect?
1)One should be able to create a tier bucket and name it.
2)Name of the bucket should be editable.
3)One should be able to add elements which can be dragged and dropped in these buckets.
4)We should be able to move an element from one bucket to another bucket.
5)May be double clicking on the 
element should bring them out of any bucket
6)Every Element will be having an image. 
so to add an element we need to provide URL 
of the image directly in a form maybe

How to add JS in your HTML pages?
There are mainly two ways to insert JS in out HTML pages.
1)To use the script tag at the
end of the body and write all the JS there.
2)Have a seperate JS file and 
then link to the HTML.


Adding JS Directly in the HTML page can lead to less readability of the code, hense this is not alwys prefered in production codebases

How to select or target element using JS?
We know that using CSS if we want to target /select
an element we can use class selector,id selctor, tag selector ect.
```CSS
#tier{
padding:5px;
}

To target an element in JS also. 
we can take help of these tag names,id's 
and classes. and thats the first function comes into the pricture.

##A Very import global object -document

Browser provides us a very important object which is globally present every where in the browser's, JS, called as document.
This object helps us to access a dom.This object has acess to the HTML, and contains a lot of methods using which we can
manipulate the DOm


##document.getElementById

So, as the name 
suggest . 
"getElementById" helps you to target a particular individual element using the id attribute of the
HTML element 

Example:
document.getElementById("tier");

The above piece of code will help you to select a element whoes id is tier

If we save this element in a variable and then on that variable try to acess a value property we will get whatever has been written inside the correseponding  element.

```JS
const tierInput=document.getElementById("tier");
console.log(tierInput);
```

Somethime we might have to fetch the text written inside the div paragraph,button etc in those cases value dones'nt help us because value is generally used to fetch the value of different input tags. if we want to fetch the text written inside an element we can use the textContent property to get and set the text of the HTML element.

```JS
const btn=document.getElementById("btn");
console.log(btn.value);// get existing text from the button
btn.textContent="Chutiya ahhe tumhi"// set a new text in the button
```

```HTML
    <button id="btn">Click Me</button>
```

##Event Driven Programming
To understant Event Driven Programming, we needs ro understand concept of event.
Events are some action or interaction done by an external agent(may be the user). on our application. For Example clicking a button is an event, scrolling is an event, moving the mouse is an event, pressing a button on keyboard is an event and so on.
So when we click a button an event is triggerd.

So react on these action, we can write logics which will only execute when these actions are triggered. And the programming paradigm where the logic/code runs as a reaction /response to these events is called as event driven programming.

##<element>.addEventListener

This method 'addEventListener'  is present on almost all of the HTML elements. We can select an HTML elements first and then call this 'addEventListener' function on that element.It takes two arguments:

1)Name of the event-example: click,dbclick,mouseover,scroll etc.
2)callback function which will be executed once the event is triggerd


```JS
const submitBtn1 = document.getElementById('clickMeButton');
submitBtn1.addEventListener('click', ()=> {
    console.log('Button Clicked');
})
```

This callback method will not be automaticlly called. it will be only called when click event happens on the submitbtn alltogether.
The addEventListener callback has a parameter called as the event object.This event object has a lot of details about the event triggered,like on which element this was triggered, what event got triggered and so on.

To get acess of the element on which the event was fired we can use the event.target property


##preventDefaut
Every event object passed in the event listener callback has a preventDefault method. which stops the dafault exection behavior of the event.
For Example: a click event on a submit button inside a form, submits the form and refreshes the page. this is the default behavior. we can avoid it by using event.preventDefault

#document.createElement

This function can help you to create brand new HTML element using JS while running the program. This function takes one argument i.e is name of the tag of the element to be craeted

The attributes of the element craeted can be modified as well.
For example. if we want to add class name to the newly created element we can use the class list property.

Note: The classList property is kind of like an array of class name attached to an html element. This property can be accessed on newly created elements and already existing elements as well

##htmlElement.appendChild
We can append a given HTML element as as child of another element by using the appendChild method

```JS
newTierList.appendChild(heading);
    newTierList.appendChild(newTierListItem);
```

1.We First create a an empty div newTierList and attach class 'tier List to it'
2.We Create a heading and and make the text contenr of the heading equal to the tierLirName parameter passed in the function.
3.We Create another div `newTierListItems` and Attach class Tier List Items to it
4. But none of this will be visible as of now. why? because these elements exists in the memory and not a part of DOm tree as of now.
5.To attach them to the DOM,we need to append them as a child of some parent.
6.First we add heading and newTierlistItem as children of new newTierList
```JS
 newTierList.appendChild(heading);
    newTierList.appendChild(newTierListItem);
```
7.We attach this 'newTierList' div as a child of 'tier-list-section'.
```JS
const tierSection = document.getElementById('tier-list-section');
    tierSection.appendChild(newTierList);
```

8. All of this logic is attched on the eventListener of click to the submit button.
```JS
const tier = document.getElementById('tier-input');
const submitBtn1 = document.getElementById('clickMeButton');
submitBtn1.addEventListener('click', (event)=> {
    alert('Button Clicked');
    event.preventDefault()
    // const trg = event.target;
    // alert(trg);
    if (tier.value === '')
    {
        alert('Please enter a tier name');
        return;
    }
    createTierList(tier.value);
    tier.value = '';
})
```

Note: 
The Alert Method create a popup for us with the message we want to show to the user.

Now let's say we want to have a seperate form which can add initial set of no-tier elements which will be later dragged and dropped

Now we can repeat the whole process just like the previous form or we can try a new approch also.

#Form Submission event
Any form when submited, emits the submit event which we can listen to and attach and event listener

```HTML
<form id="formImage">
<label for="Item" ></label>
<input type="text" id="item" placeholder="Give the item image url..."/>
<input type=submit id="submitImage" value="Submit"/>

```

```JS
const imageFo=document.getElementById("formImage");
imageFo.addEventListener('submit',(event)=>{
event.preventDefault();
console.log("Form Submited");
})
```

Creating Tier List Item for and Image

```JS
function createTierImageList(tierUrl) {
    
    const newTierImage = document.createElement('div');
    newTierImage.classList.add('tierItem');
    const image = document.createElement('img');
    image.src = tierUrl;
    newTierImage.appendChild(image);
    const tierImageSection = document.getElementById('no-tier-list');
    tierImageSection.appendChild(newTierImage);

}
```

Now the above function created the image item container and append it to the non tier section.
We should call this function on form submission

#Uderstanding More Event like drag and dropped

so drag and drop will be an event in the DOM,because it will be based on user actions.
To make an element capable of being dragged from on epart to other we have to use the draggable attribute.This attribute defines wheather the element is dragable aur not. 
Its an enumerated attribute meaning. In whaterver HTML tag we are defining this attribute we have to manually give its a "true" or "false" value as a string.

According to MDN:
    Warning: This attribute is enumerated and not Boolean. A value of true or false is mandatory, and shorthand like '<img draggable> is forbidden'.The correct usage is 
    <img draggable="false">


##Where can we add this dragable in our code.

```JS
<section id="no-tier-list">
    <div class="tierItem">
        <img src="https://plus.unsplash.com/premium_photo-1737182592549-0c83f93e2903?fm=jpg&q=60&w=3000&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8Mnx8bHV4dXJ5JTIwdmVoaWNsZXxlbnwwfHwwfHx8MA%3D%3D" alt="">
    </div>
</section>
```

Inside the non-tier-section we are having a lot of div with class item-conatiner.These divs are going to contain images of our tier-items which needs to be made draggable. SO ideally these divs should be given the dragable="true" attribute

<section id="no-tier-list">
    <div class="tierItem" draggable=true>
        <img src="https://plus.unsplash.com/premium_photo-1737182592549-0c83f93e2903?fm=jpg&q=60&w=3000&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8Mnx8bHV4dXJ5JTIwdmVoaWNsZXxlbnwwfHwwfHx8MA%3D%3D" alt="">
    </div>
</section>

But these item-container divs are craeting on the go using JS , whenever the user fills the form with an image url.
That means the newly created divs(Which are made using JS) should also contain these attributes

##setAttribute
This function is avilable on HTML element, to set a particular attribute whith a value. It takes two arguments
1)attribute name:
2)atttribute value
```JS
setAttribute(draggable,true)
```

So we can use this setAttribute function to set these dragable values

The div with class 'item-container' will be dragged so we need to add event listener to this div.
If we want to add event listener to these div, we need to select them as well.As all of them have got a common class, and we need to attach an event listner to all of these divs, we can use getElementByClassName


